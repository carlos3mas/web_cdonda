#[1m# Archivo temporal de notas sobre el script create-admin (sin datos sensibles).

Este archivo solo se conserva como recordatorio de los cambios de seguridad:
- Se eliminaron credenciales hardcodeadas.
- Ahora `scripts/create-admin.js` y `scripts/create-admin-turso.ts` usan siempre
  variables de entorno (`ADMIN_EMAIL`, `ADMIN_PASSWORD`, `ADMIN_NAME`).

No contiene ninguna contraseña ni token real.
#[m
